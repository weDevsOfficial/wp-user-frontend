# Saved logged-in sessions (Playwright storageState) — never commit credentials/cookies.
.auth/

# Per-group wp-env plugin set, generated by the e2e workflow (and handy locally
# for trimming plugins). `.wp-env.json` stays the committed default.
.wp-env.override.json
.playwright-mcp/
