{
  "name": "wpuf_automation",
  "version": "1.0.0",
  "description": "End-to-end tests for WordPress User Frontend plugin",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "CI=false npx playwright test --headed --config=playwright.config.ts",
    "test:ci": "npx playwright test --config=playwright.config.ts",

    "generate-summary": "node utils/generate-summary.js",
    
    "test:setup": "CI=false npx playwright test --config=playwright.setup.config.ts",
    "test:parallel": "CI=false npx playwright test --config=playwright.parallel-one.config.ts; CI=false npx playwright test --config=playwright.parallel-two.config.ts",
    "test:sharded": "npm run test:setup; npm run test:parallel",

    "test:setup:ci": "npx playwright test --config=playwright.setup.config.ts",
    "test:parallel:ci": "npx playwright test --config=playwright.parallel-one.config.ts; npx playwright test --config=playwright.parallel-two.config.ts",
    "test:sharded:ci": "npm run test:setup:ci; npm run test:parallel:ci",

    "sharded-summary": "node utils/sharded-summary.js"
  },
  "keywords": [ "playwright", "e2e", "tests", "wordpress", "wpuser-frontend", "wpuf" ],
  "author": "Rubaiyat E Mohammad",
  "license": "MIT",
  "devDependencies": {
    "@faker-js/faker": "^9.9.0",
    "@playwright/test": "^1.54.2",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@wordpress/env": "^10.28.0",
    "dotenv": "^17.2.1",
    "js-yaml": "^4.0.9"
  }
}
